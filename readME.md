In the following file I have created a Dockerfile , a normal dockerfile and not the distroless .
Have added the one line in the package.json "start": "vite preview --host 0.0.0.0"
and added the docker file .

will be attempting with distroless file afterwards the submission 

as the developer mention the deployed web app is running on the port 3000
